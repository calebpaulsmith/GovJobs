# Version 1 dependencies. Pin minor versions; let patches float.
streamlit>=1.36,<2.0
pandas>=2.2,<3.0
plotly>=5.22,<6.0
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0
openpyxl>=3.1,<4.0

# Geo helpers for the state map (lightweight; no GIS install required).
# Plotly has built-in choropleth support, so no geopandas in V1.

# Dev / test
pytest>=8.0,<10.0
pytest-mock>=3.14,<4.0
responses>=0.25,<1.0    # mock requests for API tests (used in Phase 3+)
