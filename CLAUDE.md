# CLAUDE.md

Guidance for Claude Code (or any AI assistant) working in this repo.

## Source of truth

`Project_Start.md` is the definitive project brief. When in doubt, defer to it. Planning docs in `docs/` elaborate on it but never contradict it.

For USAJOBS API parameters, go to the official docs first: <https://developer.usajobs.gov/api-reference/>. The local SIF guide and Excel data dictionaries are field-semantics sources, not REST parameter sources. Use `docs/USAJOBS_DATA_STRUCTURES.md` as the local reconciliation map.

For schema expansion, use `docs/SCHEMA_EXPANSION_PLAN.md` as the implementation queue and guardrail. Do not create one table per SIF field; add a table only when the plan's design rule is met: repeated per job, code-list backed, UI-filterable/groupable, scoring/application evidence, or unsafe as one scalar on `jobs`.

## What this project is

A local-first Python/Streamlit/SQLite dashboard for USAJOBS posting data and OPM workforce data. The user is the sole user. The user is not a professional coder and runs everything locally.

## Current architecture state

- Phase 1 reconnaissance is implemented in `src/data_recon.py`.
- Phase 2 SQLite persistence is implemented in `src/database.py`.
- Phase 3 USAJOBS import work lives in `src/data_import.py` plus endpoint wrappers: `src/usajobs_current_api.py`, `src/usajobs_historic_api.py`, and `src/usajobs_announcement_text_api.py`.
- Phase 4 Streamlit UI is implemented in `app.py` and `pages/`.
- Phase 5 rule-based scoring is implemented in `src/scoring.py`; current rules are documented in `docs/SCORING_RULES.md`.
- Phase 6 local alerts are implemented in `src/alerts.py`; alerts are generated manually in the app, deduped in SQLite, visible in Data Admin and Saved Jobs, and exportable as CSV. Email/push alerts are out of scope for V1.
- Phase 6.5 preference feedback and explainable similar-job recommendations are implemented in `src/recommendations.py`, backed by `job_feedback`, `recommendation_runs`, and `job_recommendations`.
- Phase 7 OPM file ingestion and map source switching are implemented in `src/opm_data.py`, `pages/7_Data_Admin.py`, and `pages/4_State_Map.py`. OPM data is file-based and must stay labeled as workforce/accessions/separations, not postings.
- Phase 8 CSV/Excel exports are implemented in `src/exports.py` and exposed for saved jobs, scorecards, and alerts.
- V2 Application Tracker is implemented in `pages/6_Application_Tracker.py`, backed by `applications` and `application_events`. It is tracking-only and must never automate applications.
- V2 Resume Versions is implemented in `pages/9_Resume_Versions.py`, backed by `resume_versions`. The dashboard itself never auto-parses, scores, or rewrites résumé contents — that posture still holds for every in-app flow. The one carve-out is the LLM Project Export (Phase 12, see `docs/LLM_EXPORT_SPEC.md` and ADR-0031), which copies user-selected résumé file(s) verbatim into a bundle the user explicitly downloads. Privacy notice + "I understand" gating in the export modal is mandatory.
- **V2 LLM Project Export is planned (Phase 12 in `docs/ROADMAP.md`, ADR-0031, full design in `docs/LLM_EXPORT_SPEC.md`).** A user-initiated export packages saved jobs (or a current Search filter, top-N by score) plus the user's résumé, locality-adjusted pay, cost-of-living context, scoring breakdown, and hiring-climate signals into a single zip the user drops into a Claude Project, ChatGPT Project, or generic chat. Specialized for federal job hunting; not a generic file packager. Targets are Claude / ChatGPT / generic only — no NotebookLM. Tier presets (Claude Pro / Claude Max / ChatGPT Plus / Pro / Team / Custom) are user-editable, dated, and labeled as guidance, not official platform limits. Preflight ingestion fetches missing HistoricJoa slices for closing-window stats with a 7-day freshness window and a wall-clock ceiling, reusing the existing `import_manifests` / `data/raw/` pattern. Implementation lives in `src/llm_export/` (vendored — small useful pieces of the sibling `Fileslicer` repo's `bundler.py` and `manifest.py` are ported with attribution; do not depend on the sibling repo at runtime).
- V2 Repost Detector is implemented in `pages/10_Repost_Detector.py`, backed by `repost_runs`, `repost_groups`, and `repost_group_members`. Treat groups as review evidence, not administrative certainty.
- Map behavior is specified in `docs/MAP_FEATURE_SPEC.md`; use that as the source of truth before changing `pages/4_State_Map.py`.
- The Public Map Tool (`thegrandpipeline.com/map`) is a separate sibling product per ADR-0016, ADR-0017, ADR-0018, ADR-0019. It lives in `public_map/` (SvelteKit + Mapbox GL JS, deployed to Cloudflare Pages) and is fed one-way by `scripts/export_public_map.py` writing static GeoJSON/JSON snapshots. The map is **layered and zoom-driven** (state choropleth → locality / county / CBSA outlines → marker clusters → individual markers). The original maxzoom of 9 was raised to **19 (street level)** on 2026-05-10 per operator request; ADR-0017's "no street-level views" stance is superseded. Polygon overlays still cap at zoom 9 so they fade out before the basemap goes street-level — only marker / FRPP layers extend to 19. The dashboard's "no cloud / no FastAPI / no React" rule still applies to everything outside `public_map/`. The detailed implementation plan lives at `C:\Users\caleb\.claude\plans\review-the-new-map-playful-wind.md`.
- Public-map reference data lives in dedicated SQLite tables: `pay_plans`, `pay_scales`, `locality_pay_areas`, `locality_pay_counties`, `counties`, `metro_areas`, `state_polygons`, `cost_of_living_index`, `data_source_status`, `locations_geocoded`, `geocoding_misses`. Polygon GeoJSON is stored as files under `data/external/` and referenced by `polygon_path`. Pay calculation goes through `src/pay_calculator.py`; status read/write through `src/data_source_registry.py`; reads through `src/reference_data.py`. Per-source ingest scripts live in `scripts/ingest_*.py` and are orchestrated by `scripts/refresh_public_map_data.py`.
- A local-only Streamlit page `pages/11_Public_Map_Admin.py` is the operator console for public-map data: per-source status, last-success time, row counts, year-over-year diffs, manual refresh, and CSV upload override. This page is part of the dashboard and never deployed.
- Reference docs for the public map: `docs/PUBLIC_MAP_DATA_SOURCES.md` (catalog of every external dataset), `docs/PUBLIC_MAP_PIPELINE.md` (operator runbook), and the Public Map section of `docs/PRODUCT_SPEC.md` (vision and definition of done).
- Phase A.7: `src/public_map_export.py` also emits `states.geojson`, `localities.geojson`, `counties.geojson`, `metros.geojson`, `pay_tables.json`, and `cost_of_living.json`; each polygon feature joins postings, OPM workforce, BEA RPP, illustrative GS-13 pay, and pay-vs-COL; markers carry `locality_code`; `manifest.json` exposes per-source freshness, layer counts, and the resolved reference year; stdlib Douglas-Peucker simplification runs at export time.
- Phase B (skeleton): `public_map/` is a SvelteKit + Mapbox GL JS app (Svelte 5, Vite 6, `adapter-static`, SPA mode). Layer order is enforced in `public_map/src/lib/layers.ts`: basemap → states fill → counties outline → metros outline → localities fill+outline → marker clusters → individual markers. Map is capped at `maxZoom: 19` (street level; raised from 9 on 2026-05-10). Polygon overlay layers (states / counties / metros / localities) keep their per-layer `maxzoom` at 9 so they fade out before the basemap goes street-level. Marker, cluster, and federal-properties layers extend through 19. The choropleth metric switcher (`public_map/src/lib/MetricSwitcher.svelte`) covers postings / workforce / accessions / separations / remote share / pay-vs-COL; `remote_share` is derived client-side because the export does not pre-compute it. Without a `VITE_MAPBOX_TOKEN` the basemap falls back to OSM raster tiles so `npm run dev` works without sign-up. Phase B exposes only a debug click-through; popups and filters land in Phase C/D.
- **Phase D.5 — Public-map course correction (2026-05-07).** The user reviewed the deployed Phase D map and flagged eleven gaps. They are now hard requirements for the public-map V1 definition of done. The implementation queue lives in the "Phase D.5" section of `C:\Users\caleb\.claude\plans\review-the-new-map-playful-wind.md`. New ADRs covering the course correction: ADR-0024 (UI invariants — heat-map persistence, panel layout grid, agency typeahead, saved searches, address zoom), ADR-0025 (Federal Real Property layer), and ADR-0027 (self-bootstrapping ingests). Until D.5 is closed, the map is "under construction" — every metric in the metric switcher must declare a status (`ready` | `wip` | `under-construction`), and any metric whose data is empty must show `under-construction` with a tooltip explaining why, never a silently broken color ramp.
- **Phase D.5 follow-up requirements (2026-05-08).** ADR-0028 expands V1.5 with official 2026 GS pay tables, snap-scoped state/locality/future-city search windows, Add to Search geography chips, scoped with/without-global-filter previews, light/dark mode, personal compensation/COL comparison, source-backed job urgency/fill badges, and a local browser profile for viewed/saved/hidden jobs. These are D.5.14-D.5.19 in the detailed plan.
- **Phase D.5.-1 (shipped 2026-05-08).** Self-bootstrapping ingest pipeline per ADR-0027: every reference-data ingest now defaults to a known canonical URL or a checked-in seed CSV when no `--input` / env var is supplied. Implemented via `src/ingest_common.py::resolve_or_download`, `shapefile_zip_to_geojson`, `ensure_geojson_input`, and `write_geojson` (depend on `pyshp` per `requirements.txt`). State / county / CBSA polygon ingests download Census TIGER 2023 ZIPs on first run and cache them under `data/external/<source_key>/`. OPM locality definitions, OPM locality pay percentages, OPM GS base pay table, and BEA RPP all default to checked-in seed CSVs under `data/external/opm_locality_definitions/2025.csv`, `opm_locality_pay/2025.csv`, `opm_gs_pay/2025_base.csv`, and `bea_rpp/2023.csv`. The locality polygon ingest auto-discovers the most recent year present in `locality_pay_counties` and silently falls back from ArcGIS to county-dissolve when ArcGIS yields zero recognized features. The orchestrator (`scripts/refresh_public_map_data.py`) enables every source by default; env vars are overrides, never enablement gates. A clean checkout running `refresh_public_map_data.py && export_public_map.py` now produces a manifest with non-zero counts for every polygon layer.
- **Pay-vs-COL formula corrected (2026-05-08).** `src/public_map_export.py::_pay_vs_col` now produces a purchasing-power index where 100 = U.S. average, calculated as `(locality_pay ÷ national_base_pay) ÷ (rpp ÷ 100) × 100`. The previous `pay ÷ rpp × 100` formula produced values in the 100,000s that all clamped to the choropleth's top color. The reference national base pay (GS-13 step 1 base for the resolved year) is fetched once per export via `_gs_base_step1_for_year` and threaded into `states_geojson`. Existing tests preserve the legacy formula when `national_pay` is omitted so they keep passing. See [DECISIONS.md ADR-0027](docs/DECISIONS.md) for context.
- **Public-map UX additions (2026-05-08).** New invariants in `public_map/src/lib/`:
  - `mapState.choroplethEnabled` (in `store.svelte.ts`): clicking the active metric pill in the MetricSwitcher toggles state-fill shading off; clicking any other pill flips it back on. The Map's `setChoroplethVisible` helper drops `fill-opacity` to 0 without removing the source so polygon clicks still work.
  - `mapState.postingHeatEnabled` (in `store.svelte.ts`): the D.5.1 heat surface is a dedicated unclustered `jobs-heat` source plus `posting-heat` heatmap layer, updated from the same filtered job collection as markers. Keep this source separate from clustered markers so heat density is not distorted by Mapbox clustering.
  - `mapState.closedJobsEnabled` (in `store.svelte.ts`): the D.5.7 trailing-90-day overlay comes from `closed_jobs.geojson`, exported by `src.public_map_export.closed_jobs_geojson`. It renders as muted gray points at zoom >= 7 and is off by default. Treat it as context only; never as an active apply target.
  - `mapState.listView` (in `store.svelte.ts`): when set, the FeaturePanel renders `JobList.svelte` instead of the selected feature's detail. The state roundup's "View N postings" button populates this with `{scope: 'state', code, label}`. Filter chips are honored via the existing `filterJobs(allJobs, mapState.filters, details)` pipeline.
  - `InfoTooltip.svelte`: reusable hover/focus/click ⓘ component for calculation traceback. Used in `StateRoundup.svelte` against `Locality`, `GS-13 step 1`, `RPP`, and `Pay/COL index` — each tooltip shows the formula, the actual numbers used, and the data source. Reuse this component before introducing any new tooltip pattern.
  - `FilterPanel.svelte`: auto-collapses to its toggle button when `mapState.selectedFeature` or `mapState.listView` becomes non-null, then stays collapsed (the user can manually re-expand). Prevents overlap with the right-side feature panel.
- **Public-map D.5.2/D.5.3 shipped (2026-05-09).** Agency filtering is now a code-backed multi-select in `FilterPanel.svelte` fed by `agencies.json`; URL state uses repeated `agency=` keys and free text does not filter until committed as a chip. Saved searches are client-only via `public_map/src/lib/SavedSearchMenu.svelte` and `savedSearches.ts`, stored under `localStorage` key `tgp.public_map.saved_searches.v1`; each saved item carries filters, metric, viewport, and the last address-zoom target when present. Apply restores filters, metric, address target, and viewport through `mapState.addressTarget` / `mapState.pendingViewport`.
- **Public-map D.5.4 shipped (2026-05-09).** Address / ZIP zoom has downstream UI in `public_map/src/lib/AddressSearch.svelte` and geocoder logic in `geocode.ts`: Mapbox first when `VITE_MAPBOX_TOKEN` is present, Nominatim fallback, and offline lookup from `zip_centroids.json`. `mapState.addressTarget` drives `Map.svelte` `flyTo` behavior and a transient `address-pin` source/layer in `layers.ts`; the address pin is not part of the job marker source, heat source, or click router. Upstream support is `zip_centroids` in `src/database.py` (schema version 10), `scripts/ingest_zip_centroids.py` using the Census 2024 ZCTA Gazetteer by default, `src.public_map_export.zip_centroids_payload`, and `scripts/export_public_map.py` writing `zip_centroids.json`. The latest local export wrote 33,791 ZIP/ZCTA rows.
- **Public-map D.5.5 initiated (2026-05-09).** `Map.svelte` polygon clicks now fit bounds for state/locality/county/metro features and set `mapState.focusedArea`; a Back to national pill clears focus and flies back to `[-97, 38.5], 4`. The FeaturePanel header uses the concise layer label `State` while `StateRoundup.svelte` keeps the content eyebrow, avoiding duplicate "State roundup" text.
- **Public-map D.5.6/D.5.18/D.5.19 shipped (2026-05-09).** D.5.6: `MetricDef` gains `status: 'ready'|'wip'|'under-construction'` + `wipNote`; `Map.svelte::computeMetricDemotion` runs after states load and sets `mapState.demotedMetrics`; `MetricSwitcher.svelte` shows striped-placeholder + note for wip metrics, hides under-construction metrics behind "Show experimental". D.5.18: `format.ts::urgencyBadge` returns `{text, level: 'critical'|'soon'|null}` from `close_date`; colored pill badge appears in JobCard (below title) and JobList rows. D.5.19: `jobProfile.svelte.ts` singleton persists viewed/saved/hidden in `localStorage` under `tgp.public_map.job_profile.v1`; `ProfileDrawer.svelte` is a right-side drawer with Saved/Hidden/Viewed-Closed tabs; `mapState.hiddenJobIds` + `excludeHidden()` filter hidden jobs from all sources on every filter/hide cycle; JobCard marks viewed on mount and has Save/Hide buttons; "My Jobs" pill in the masthead opens the drawer with a saved-count badge.
- **Public-map D.5.17 compensation/COL comparator shipped (2026-05-09).** Per ADR-0028. New helper module `public_map/src/lib/compensation.ts` exposes `compute()` plus `gsBasePay`, `applyLocality`, `localityPrimaryState`, `localitiesFromGeoJson`, and `stateRppFromCol`. `CompensationComparator.svelte` is a right-hand drawer (overlay + slide-in panel, same pattern as `ProfileDrawer.svelte`) launched by the new `Pay Compare` pill in the masthead via `mapState.compareOpen`. GS mode multiplies `pay_tables.json` GS base by the OPM locality adjustment from `localities.geojson`; custom mode takes a wage in a chosen state. Comparison uses BEA RPP from `cost_of_living.json` (loaded via the new `loadCostOfLiving()` helper in `data.ts`). Source RPP is flagged approximate when the OPM locality spans multiple states (the locality's primary state is used). Calculation surfaces the formula and source citations inline. Tests live in `compensation.test.ts` (18 cases, vitest). Do not introduce a separate calculator path inside JobCard or any other component — reuse `compute()` for all locality/state RPP math.
- **Public-map D.5.9 federal-properties layer shipped (2026-05-09).** Per ADR-0025. New schema v11 table `federal_properties` (frpp_id PK + state/agency_code indexes) seeded by `scripts/ingest_federal_properties.py`, which is self-bootstrapping per ADR-0027 with a checked-in seed at `data/external/gsa_federal_properties/seed.csv` (30 well-known FRPP buildings spanning 17 states; replaceable via `--input` or `PUBLIC_MAP_FRPP_CSV`). `src.public_map_export.federal_properties_geojson` emits a Point FeatureCollection with non-null properties only; `scripts/export_public_map.py` writes `federal_properties.geojson` and adds the count to `manifest.json.layers`. UI: `SOURCE_IDS.federalProperties` + `LAYER_IDS.federalProperties` (a `circle` layer at `minzoom: 6`, off by default), toggled via `mapState.federalPropertiesEnabled` and a "Fed props" pill in `MetricSwitcher`. `Map.svelte` wires a `loadFederalProperties()` source, gates clicks on the toggle, and routes selections to a new federal-property branch in `FeaturePanel.svelte` that surfaces name / agency / address / status with a "Source: GSA FRPP" footnote. Tests in `tests/test_ingest_scripts.py` and `tests/test_public_map_export.py` cover ingest normalization, replace-all, seed integrity, geojson shape, null-skipping, and out-of-range coords.
- **Public-map D.5.24 on-demand Posting Intelligence shipped (2026-05-09).** Per ADR-0029. New Cloudflare Pages Function at `public_map/functions/api/job-history.ts` proxies USAJOBS HistoricJoa (a public, key-less endpoint) with a 24-hour edge cache (5-minute TTL on failures) and a 5-page upstream cap (~2,500 records); `truncated: true` is set when the cap fires. Pure helpers in `public_map/src/lib/jobHistory.ts` (`WINDOW_KEYS`, `WINDOW_LABELS`, `windowToDateRange`, `buildHistoricJoaParams`, `trimRecord`, `postFilter`, `bucketByMonth`, `cacheKey`, `normalizeWindow`) are imported by both the worker and the Svelte client so both ends of the contract agree. UI: `PostingIntelligence.svelte` mounts collapsed at the bottom of `JobCard.svelte`; opening it calls `/api/job-history` with `mapState.filters` (agency/series/grade/state) plus the host posting's identifying fields as the upstream query, then renders an inline SVG monthly bar timeline with seven window pills (`1mo` / `3mo` / `6mo` / `1yr` / `3yr` / `5yr` / `10yr`) and a paginated drill-in list (25 rows/page). The component never writes back to `mapState.filters` — all geographic/agency/series chip mutations remain `FilterPanel`/`QuickAdd`'s job. Failure mode renders an explicit "history unavailable" message with the upstream error text; never fabricated data. Tests: 21 vitest cases in `jobHistory.test.ts`. Bulk HistoricJoa imports remain rejected for the public map per ADR-0029 / CLAUDE.md hard rule 22.
- **Public-map D.5.10 county-level COL shipped (2026-05-09).** Per the `census_acs_rent` entry in `docs/PUBLIC_MAP_DATA_SOURCES.md`. Ingest is `scripts/ingest_acs_county_rents.py`, self-bootstrapping per ADR-0027 with a checked-in seed at `data/external/census_acs_rent/2023.csv` (~40 well-known counties + ~26 state median-rent rows). The script reads state and county median rent from the CSV, joins each state's BEA RPP from `cost_of_living_index`, and writes `geo_type='county'` rows with `rpp_overall = state_rpp × (county_rent / state_median_rent)` and `source='census:acs5_b25064'`. State rows from the CSV are read for their median-rent values but never inserted (BEA already covers states). Counties whose state has no BEA row are skipped — the exporter's state-level fallback still kicks in for them. `src.public_map_export.cost_of_living()` now emits `by_county` alongside `by_state` and `by_cbsa`. `counties_geojson()` prefers the county-level RPP over the state fallback and exposes `rpp_overall_source: 'county' | 'state'` on every feature, plus `gs13_step1_locality` and `pay_vs_col` so the CountyDetail popup can render the same Pay/COL math it already shows for state and locality. `CountyDetail.svelte` reads `rpp_overall_source` and labels precision honestly ("County (ACS rent-derived)" vs "State fallback") in both the value cell and the calculation tooltip. Orchestrator step `ingest_acs_county_rents` runs after `ingest_bea_rpp` so the state RPP join is populated. Override via `--input` or env var `PUBLIC_MAP_ACS_RENT_CSV`. Tests cover the derivation formula, the missing-state-RPP skip, the seed sanity, and both the by_county payload and the counties_geojson precedence.
- **Public-map D.5.11 + D.5.14 cutover (2026-05-09).** D.5.14 — 2026 GS pay-table cutover infrastructure shipped per ADR-0030: checked-in seeds at `data/external/opm_gs_pay/2026_base.csv` (computed from 2025 base × 1.0% across-the-board raise per the OPM PDF title "Incorporating the 1% General Schedule Increase"), `data/external/opm_locality_pay/2026.csv`, and `data/external/opm_locality_definitions/2026.csv` (locality % and county→locality mappings carried forward as 2026 placeholders). `SEED_CSV` constants in the three OPM ingest scripts now point to the 2026 files; `current_reference_year(conn)` resolves to 2026 once seeds are imported; the orchestrator label changed from "2025 seed" to "2026 seed". `pages/11_Public_Map_Admin.py` gained a "Reference year (D.5.14)" panel that shows whether the resolved year matches the V1 target, samples three GS cells (GS-1 step 1, GS-13 step 1, GS-15 step 10), and instructs the operator to verify them against the official OPM 2026 PDF and replace the seed if any cell differs by more than $1. **D.5.14 stays partial in ROADMAP until that operator verification pass is logged.** D.5.11 — exact pay tables on every job: `src.public_map_export._build_pay_grid` runs `pay_calculator.calculate_job_pay_table` against each job's first listed location and embeds `{status, year, pay_plan, locality, method, grades, notes, missing_reason}` in `jobs_detail.json`. Status flag: `exact` when every cell came from a locality-specific `pay_scales` row, `approximated` when at least one cell was derived as `base × (1 + pct)`, `unavailable` when the job lacks a pay plan/grade or `pay_scales` has no matching rows. `JobCard.svelte` renders a colored status pill (exact / approximated / snapshot) next to the pay-table heading and a dashed-border "Pay scale not yet ingested — see admin" panel with a link to the admin page when status is `unavailable`. The global `pay_tables.json` lookup remains as a backstop for older bundles produced before D.5.11.
- USAJOBS field mapping lives in `src/usajobs_normalize.py`. Do not duplicate normalization logic inside UI pages or importers.
- Preference learning / recommendations must remain inspectable: user feedback accepts both a structured signal (`liked`, `disliked`, `more_like_this`, `less_like_this`) and a short free-text explanation for later review. Suggested jobs must always expose "why suggested" factors; do not add opaque recommendations.

## USAJOBS data layers

- `/api/Search` is the current live listings layer. It requires USAJOBS credentials and is capped by USAJOBS Search limits. Use this for "what can I apply to right now?" For agency targeting, use `Organization=<agency subelement code>`; FEMA is `Organization=HSCB`.
- `/api/historicjoa` is the structured archive layer. It is public bulk data with continuation-token paging. Use it for the local historical database, trend charts, maps, and scorecards. For agency targeting, use `HiringAgencyCodes`; FEMA is `HiringAgencyCodes=HSCB`.
- `/api/historicjoa/announcementtext` is the long-form selected text layer. Pull it selectively, not wholesale at startup. For agency/date slices, use the same filters as HistoricJoa instead of one request per control number.
- The two most important long-form fields for matching are `summary`, stored as `job_text.summary`, and `requirementsQualifications`, stored as `job_text.qualifications`.
- `summary` is the top description of what the job is. `requirementsQualifications` contains the GS-equivalency / specialized-experience language.
- Prefer filter-based UI/import scopes over keyword search. Keyword is useful for discovery, but agency code, department code, series, grade, date, location, and hiring-path filters should drive reproducible imports.
- Current `jobs` rows are flattened summaries. USAJOBS/SIF data contains repeated locations, series, hiring paths, and required documents; Phase 4.5 stores those in child tables before broad imports or serious scoring.
- Implemented schema-expansion tables include `job_import_scopes`, `job_grades`, `job_salary_ranges`, `job_requirements`, `job_qualification_requirements`, `job_duties`, `job_evaluation_factors`, `job_openings`, `job_contacts`, `job_security_clearances`, `job_travel_requirements`, and `job_application_options`. Media/cyber/vendor/SIF-transaction tables should wait until an actual product workflow needs them.

## Hard rules

1. **Never hardcode API keys.** Credentials live only in `.env` (read via `python-dotenv`). `.env` is gitignored; `.env.example` is the template.
2. **Postings â‰  hires.** Use "postings" or "announcements" for USAJOBS data and "hires", "accessions", "separations", or "workforce counts" for OPM data. Charts and maps must label the data source.
3. **No full download without reconnaissance.** Before downloading any large dataset, run `src/data_recon.py` and write the recommendation to `docs/DOWNLOAD_STRATEGY.md`. The user explicitly approves the mode (`FULL_DOWNLOAD` | `FOCUSED_FULL_DOWNLOAD` | `STAGED_DOWNLOAD` | `SAMPLE_ONLY`).
4. **Respect rate limits.** Implement paging, backoff, retry, and resumable imports. Save every raw response to `data/raw/...`. Track each request in `raw_api_responses` and each import run in `import_manifests`.
5. **Deduplicate.** Use position ID + announcement number + source as the dedup key for jobs. Upserts only â€” never blind insert.
6. **Transparent scoring in V1.** The match-score module is rule-based, returns a 0â€“100 score plus a list of positive factors, negative factors, and missing info. **Do not call an LLM for scoring in V1.**
7. **No advanced features in V1.** No React, FastAPI, Docker, cloud deployment, user accounts, vector DB, paid APIs, browser scraping, or auto-applications. See `docs/ROADMAP.md` for what belongs where. **Exception:** the `public_map/` subdirectory is a separate sibling product per ADR-0016 (a static, read-only SvelteKit site at `thegrandpipeline.com/map` fed by nightly snapshots). The dashboard itself stays local-first and unchanged.
8. **Tests live next to features.** Anything in `src/` has a matching `tests/test_*.py`. Use mocked HTTP responses; do not hit real USAJOBS endpoints in tests.
9. **Preserve control numbers.** Keep `usajobs_control_number` on imported jobs; it is the join key between HistoricJoa structured rows and selected AnnouncementText rows.

## Public Map V1.5 invariants (course correction)

These rules apply to every change inside `public_map/` and to the data flowing into it from `scripts/export_public_map.py`. They override anything in the original Phase A–F plan that conflicts. Source: user review on 2026-05-07.

1. **Agency input is a code-backed multi-select.** No raw text box. Typing `FEMA` matches by `agency_code`, `agency` (display name), and `department`, with the canonical entry surfaced from `agency_codes` (joined with `agencies.json` posting counts). The user must commit a chip from the dropdown — free-text without a chip submits nothing. Multiple agencies are ANDed with the rest of the filters and ORed across the chips. URL state uses repeated `agency=HSCB&agency=NTSB` keys, not a comma-joined string.
2. **Saved searches are first-class.** Every filter set (including the active metric, address-zoom target, and chip list) can be named, saved, listed, recalled, and deleted from the UI. V1 storage is `localStorage` keyed by user-provided name, schema-versioned. URL sharing already works (Phase D); saved searches are the local equivalent for the same person across visits.
3. **Address / ZIP zoom-in.** A geocoder input near the masthead accepts a US street address, city/state, or ZIP code, geocodes via Mapbox Geocoding API (`mapbox.places`) when a token is present and via Nominatim as the no-token fallback, then `flyTo`s with a sensible zoom (state→5, metro→7, ZIP→9). Result is **never** plotted as a job marker — it's a transient flag pin that disappears on the next interaction.
4. ~~A heat surface is always visible.~~ **(Removed 2026-05-10.)** The posting heat-map layer was archived to `public_map/src/lib/_archived/heatmap/` because its intensity radius did not visually correlate with marker positions at zoom 4–7 (operator review). Density visualization, if revived, should be a hex-grid kernel density driven by raw points rather than a per-marker heat radius — see the archive's README for revival instructions.
5. **Click a state → fit bounds to that state.** State-fill click no longer just opens a popup — it fits the map to the state's polygon bounds (with padding) **and** opens the State Roundup. Same gesture for localities, counties, and CBSAs at their respective zoom bands. A "back to national" pill appears when the user is below zoom 5 inside a state filter.
6. **The metric switcher is honest.** Each metric carries `status: 'ready' | 'wip' | 'under-construction'`. Only `ready` metrics render a working choropleth. `wip` shows a striped placeholder fill, an explanatory caption, and a "no comparable data yet" legend. `under-construction` is hidden behind a "Show experimental metrics" toggle. The default metric is the postings heat map (now zoom-persistent per #4) plus a state choropleth of `postings`. Any metric whose feature property is null for ≥ 50% of states is automatically demoted to `wip`.
7. **All four polygon layers must actually render.** `manifest.json.layers` must report non-zero counts for `states.geojson`, `localities.geojson`, `counties.geojson`, and `metros.geojson` before D.5 is callable done. The exporter prints a warning when any layer comes back empty so the operator can see it locally before pushing.
8. **Federal property points are a layer, not a marker mode.** Per ADR-0025, GSA Federal Real Property Profile (FRPP) buildings ingested into a new `federal_properties` table feed `federal_properties.geojson`, rendered as small neutral diamonds at zoom ≥ 6. Job markers and federal-property points coexist; they are visually distinct and have separate panels. Ingestion via `scripts/ingest_federal_properties.py` and tracked in `data_source_status`.
9. **Cost of living layer goes deeper than state.** Counties carry a county-level COL via ACS median rent + BLS CPI metro joined to county FIPS (or, when only state RPP is present, a state fallback labeled "approximate"). Locality popups show RPP from constituent metros (already done) and the county breakdown when a county is hovered.
10. **Pay tables: GS today, per-plan in the future; never widened.** Today the JobCard renders GS-family pay only; non-GS plans show the posting's salary range. The per-plan rendering described below is the **target design direction (ADR-0032, Proposed)** and is not yet scheduled — see the Open Questions in ADR-0032. The **no-widen guard at the end of this rule binds immediately** regardless of schedule. The target design, when built:

    Each `pay_plans.code` declares a `pay_resolution_strategy` and the JobCard renders accordingly:
    - **GS-family** (`GS`, `GM`, `GL`, `GP`, `GR`, hybrid Title 38 GS-coded postings, and `LE` LEO table where applicable): full grade × step locality-adjusted table from `pay_scales`. Status pills keep their D.5.11 meaning — `exact` when every cell came from a locality-specific row, `approximated` when at least one cell was derived as `base × (1 + pct)`. If `pay_scales` is missing rows for the `(pay_plan, year, locality, grade)`, the JobCard shows "Pay scale not yet ingested for this plan/year — see admin" with a link to `pages/11_Public_Map_Admin.py`. No silent blanks.
    - **Range-only plans** (`ES`, `SL`, `ST`, `EX`, AcqDemo `NH` / `NJ` / `NK`, FAA `AT`, TSA `SV`, other broadband demos): card shows the statutory range or broadband min/max for the year from a per-plan range source (or the posting's own `salary_min` / `salary_max` when no published range exists), labeled with the plan's name and "no step structure."
    - **Wage-area plans** (`FW` and the underlying `WG` / `WL` / `WS`): until an FWS wage-area layer ships, card shows "Federal Wage System — wage area not yet modeled in V1" alongside the posting's salary. Do **not** stamp an FWS posting with a GS locality polygon or compute its pay via GS adjustment %.
    - **Facility / market plans** (Title 38 `VN` / `VM` / `VP` and other VHA codes; foreign service `FO` / `FP`): card shows the posting's salary plus a one-line note pointing to the VA pay schedules / post-differential source. No fabricated table; no GS locality stamp.
    - **Administratively determined** (`AD`): posting salary only, with "Agency-determined; no published table."
    - **Unknown / unregistered plan codes**: posting salary only with "Pay system not modeled," surfacing the raw plan code so the operator can register it.

    The `unavailable` status is reserved for GS-family postings that *should* have a table but don't yet — never for plans that are intentionally out of the table model. State / county pay-vs-COL choropleth and the GS-13 step 1 reference (`_gs_base_step1_for_year`, `_pay_vs_col`) remain GS-family only and must be labeled as such in legends and tooltips; non-GS jobs do not feed the choropleth.

    **`pay_scales` is for table-shaped pay only — do not widen it to fit other plans.** The composite key `(pay_plan, year, grade, step, locality_code)` exists because GS-family pay actually has that shape. Do not fabricate `step=0` rows for broadband plans, do not stuff FWS wage-area rates under fake GS locality codes, do not insert facility-level VA rates with synthetic locality codes, and do not insert AD or foreign-service rates at all. Range-only plans live in a separate `pay_ranges` table (or columns on `pay_plans`); FWS lives behind its own wage-area path when Phase 13 lands; facility and market pay live in their own per-plan tables. Adding a new pay system to V1 means either it fits `pay_scales` honestly *or* it gets its own resolution path — never both, never neither. PRs that grow `pay_scales` rows for non-GS-family plans must be rejected and the work routed to the appropriate per-plan path; an ADR is required before introducing any new pay-area kind or per-plan rate table.
11. **OSM fallback is supported, not aspirational.** When `VITE_MAPBOX_TOKEN` is unset, the OSM-raster style must (a) load without a Mapbox account, (b) avoid `glyphs:` URLs that require auth, (c) skip Mapbox telemetry (`mapboxgl.config.SEND_EVENTS = false`), and (d) render the same layer stack as the Mapbox style. Tests must boot the map under both modes. If the tradeoffs of `mapbox-gl` v3 prevent the no-token path, switch the public map to `maplibre-gl` (drop-in API, no token requirement); record the choice in an ADR before doing so.
12. **No element overlap.** UI panels live on a fixed grid:
    - top-center: masthead (slim pill)
    - top-left: address-zoom search + saved-searches dropdown (single column)
    - middle-left, below the search: filter panel (collapsible drawer)
    - middle-right: feature/detail panel
    - bottom-center: metric switcher (legend + status pill)
    - bottom-left: data-source freshness + attribution
    - bottom-right: native Mapbox controls
    On screens < 720 px, the filter and feature panels collapse into a single bottom drawer. Any new component must declare its grid cell in `public_map/src/lib/layout.ts` (a new module added in D.5.0); ad-hoc absolute positioning is not allowed.
13. **The map needs a real corpus to evaluate.** Until the dashboard reliably holds ≥ 5,000 open USAJOBS postings, every public-map review session is meaningless. A corpus-growth task is part of D.5 (D.5.7): expanded import scopes from the dashboard's Data Admin page, OPM workforce file ingestion, and a "recently closed" overlay built from HistoricJoa for the trailing 90 days. Closed historical postings are rendered as faint gray dots distinct from open markers and labeled "closed within 90 days, not applicable."
14. **Ingest scripts are self-bootstrapping (per ADR-0027).** Every reference-data ingest must run successfully from a clean checkout without the operator setting any environment variable. Env vars (`PUBLIC_MAP_*_GEOJSON`, `PUBLIC_MAP_BEA_RPP_CSV`, etc.) are **override** paths, not **enablement gates**. The orchestrator (`scripts/refresh_public_map_data.py`) enables every supported source by default and either downloads from a known canonical URL (Census TIGER, BEA RPP) or reads a small checked-in seed CSV (`data/external/opm_locality_definitions/<year>.csv`, etc.). A "skipped: env var not set" line is a bug, not a feature.
15. **Public-map V1 uses 2026 GS pay tables.** The checked-in 2025 OPM seeds are bootstrap/dev data only. Before V1 is callable done, `pay_scales` must contain official 2026 GS base rows and locality rows, `manifest.json.reference_year` must resolve to 2026, and the admin diff must verify sampled 2026 rows against OPM-published values.
16. **Polygon clicks snap and open a scoped action window.** Clicking a state or locality fits the map to that polygon and opens a compact scoped window. The same contract applies to the future city polygon layer. Each window includes: Search this area, Include remote, Add to Search, and a toggle to preview with global filters applied or temporarily ignored.
17. **Geographic search scopes are additive.** `Add to Search` on a state, locality, or city adds a geography chip to the current search instead of replacing the user's existing filters. Multiple geography chips are ORed with each other and ANDed with non-geographic filters; the scoped window can show counts both with and without global filters.
18. **Light and dark modes are first-class.** The public map has a visible light/dark toggle, persists the user's choice locally, and applies the theme to panels, controls, map overlays, and the selected basemap style. Both modes must pass the no-overlap and legibility checks.
19. **Personal compensation comparison is required.** Users can enter either a GS grade/step plus locality or a custom annual wage. They can optionally enter a current comparison city. The map converts that pay into cost-of-living-adjusted equivalents, e.g. "GS-13 Step 2 in Denver is equivalent to making $X in Chicago," with source labels and approximation flags.
20. **Job cards surface urgency and fill signals.** Listings show clear badges when a posting is closing soon, closing tomorrow/today, or has a known applicant/opening fill signal. Use `close_date`, `job_openings.total_openings`, and any USAJOBS application-count fields that are actually present; never scrape or invent applicant counts when the source does not provide them.
21. **Viewed, saved, and hidden jobs are tracked locally.** Opening a job marks it viewed everywhere in the public map. `Save Job` and `Hide Job` are first-class actions on JobCard and JobList rows. Hidden jobs disappear from map markers, heat inputs, lists, and scoped-search counts by default, but remain recoverable from a local profile drawer alongside Saved Jobs and Viewed Jobs That Have Closed. V1 storage is localStorage/IndexedDB only; no public-map accounts or server sync.
22. **Historic posting data is on-demand, edge-cached — never bulk-imported (per ADR-0029).** The "Posting Intelligence" tab on JobCard is click-to-load only and proxies USAJOBS HistoricJoa via `public_map/functions/api/job-history.ts` with a 24-hour edge cache. Bulk HistoricJoa imports are rejected for the public map: the local SQLite must not grow to millions of historic rows just to power per-job features. The trailing-90-day closed-jobs overlay is the *only* historic data shipped in the static bundle, because it is used at every page-load by the gray-dots layer. Anything per-job, per-agency, or per-filter pulls live and caches at the edge. When HistoricJoa is unavailable, the tab shows an explicit "history unavailable" message — never a fake-history fallback.
23. **The scheduled bundle refresh must be conflict-aware.** `.github/workflows/refresh-public-map.yml` is the only automation allowed to push commits that touch `public_map/static/data/*`. Before it pushes, it must `git fetch && git rebase origin/master`; if the rebase conflicts, the only conflicting paths can be the auto-generated bundle files, so the workflow re-runs `python scripts/export_public_map.py`, stages the regenerated bundle, and continues the rebase. Non-bundle conflicts must abort the workflow with a loud error — they indicate something the operator needs to resolve by hand. Retry the rebase + push up to three times before failing.
24. **Local bundle conflicts are resolved with a script, not by hand.** When the operator pulls and the *only* conflicts are under `public_map/static/data/`, they run `python scripts/resolve_bundle_conflicts.py`. The script regenerates the bundle from the local SQLite, stages it, and continues the merge or rebase. The script must refuse to run if anything outside `public_map/static/data/` is conflicted, and must refuse to run when no merge or rebase is in progress.
25. **The Public Map Admin page must lead with a freshness headline.** [pages/11_Public_Map_Admin.py](pages/11_Public_Map_Admin.py) renders, as its first content under the title, a row showing: time of the last completed `import_manifests` row (with row count and endpoint), open USAJOBS count, total USAJOBS count, bundle generated-at age, and bundle reference year. If the most recent import completed *after* `manifest.json.generated_at`, the page must show a warning that the bundle is stale and that the operator should re-export. Operators should never have to scroll past a working bundle to learn whether it reflects current local data.

## Build order (strict)

1. Planning docs (this phase).
2. `src/data_recon.py` + `src/logging_utils.py` + tests.
3. `src/database.py` + tests.
4. USAJOBS importers (`src/usajobs_current_api.py`, `src/usajobs_historic_api.py`, `src/usajobs_announcement_text_api.py`, `src/data_import.py`) + tests.
5. Streamlit app + pages.
6. Phase 4.5 data structure hardening: agency/code tables, repeated-structure child tables, and filter-first import/UI scopes.
   - Follow `docs/SCHEMA_EXPANSION_PLAN.md` for table priority and "do not model everything" rules.
7. `src/scoring.py` + tests. **Done for V1 `v1.0`; update `docs/SCORING_RULES.md` when changing weights.**
8. `src/alerts.py` + tests. **Done for local/manual V1 alerts; no email yet.**
9. Recommendation feedback/similarity. **Done for deterministic local recommendations; embeddings remain V3.**
10. OPM importer and map source switch. **Done for file import and state-level workforce/accession/separation maps.**
11. Maps, scorecards, exports. **Done for CSV/Excel exports of saved jobs, scorecards, and alerts.**
12. V2 tracking/intelligence: Application Tracker, Resume Versions, and Repost Detector are implemented. Remaining V2 starts with closing-window analytics and the LLM Project Export (Phase 12 in `docs/ROADMAP.md`; full design in `docs/LLM_EXPORT_SPEC.md`; ADR-0031). RAG/vector matching remains V3.

Do not skip ahead.

## Coding conventions

- Python 3.11+. Type hints on all public functions.
- Module-level `logger = logging.getLogger(__name__)`. No `print()` in `src/`.
- Functions that hit the network take a `dry_run: bool = False` and a `max_pages: int | None = None` parameter for testability.
- Database access goes through `src/database.py`. Pages and importers do not write SQL directly.
- All times stored as ISO-8601 UTC strings. Convert at the edge.
- Filenames in `data/raw/` follow `{source}/{endpoint}/{YYYYMMDD}/{query_hash}_{page}.json`.
- Tests mock HTTP with `responses` or pytest `monkeypatch`; tests must not make real network calls.

## When the user asks to add something not in the roadmap

Push back. Ask whether it belongs in V1, V2, or V3 and update `docs/ROADMAP.md` and `docs/DECISIONS.md` before coding. Cleanup of the roadmap is preferable to surprise scope creep.

## Things to ask before coding, not after

- Is the dataset within the configured size thresholds?
- Does the importer have a resumable manifest entry?
- Are tests written first or alongside?
- Has the field been added to `docs/FIELD_DICTIONARY.md`?
- If adding a table, does `docs/SCHEMA_EXPANSION_PLAN.md` say it is necessary now?

## Useful references

- USAJOBS API: <https://developer.usajobs.gov/api-reference/>
- USAJOBS rate limits: <https://developer.usajobs.gov/guides/rate-limiting>
- OPM data: <https://data.opm.gov/>
- FedScope: <https://www.opm.gov/policy-data-oversight/data-analysis-documentation/fedscope/>
